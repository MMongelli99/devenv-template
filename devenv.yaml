clean:
  enabled: true
  keep:
    # - name # allow to set devenv shell name
    - EDITOR # allow user to keep their expected/preferred editor
    - TERM # terminal history doesn't clear upon vim exit with default 'dumb'
    - TERMINFO # fix 'terminal is not fully functional' warning
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  # nixpkgs-python:
  #   url: github:cachix/nixpkgs-python
  #   inputs:
  #     nixpkgs:
  #       follows: nixpkgs
  nvf:
    url: github:notashelf/nvf


  
